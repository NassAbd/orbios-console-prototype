from ben
check open pbs
minimize data transfer
not real time
simulate satellites range
military mfd aircraft
raspberry pi
track satellites by json
simulator to start the fire

simulate temperature, heat, impact

ai able to manage container

console with 3 windows

satellites demonstrate where it is, acquisition of signal

console with system status, queue/tasks status

simulator with button

everything's modular

not rewriting everything

see what data status we can have from raspberry pi

make the mock as much modular as possible, for the connection to other parts be the easiest

list of what's running
status
what do we need from the AI
connect to workstation with ssh


if wildfire detection signal appears
then
    validate the signal
    if signal is valid
    then
        suspend non priority OS functions
        extract geolocation coordinates
        generate timestamp
        create alert message string
        send message to ground station
    else
        ignore the signal
        resume normal OS functions
else
    continue normal os functions


Replace hardware metrics and proccesses from my mac to mock raspberry pi running linux ubuntu

Use case calls TIR, VNIR/ vehicle calls

Show differences of prioritization of tasks
Image envoyé à gs -> console

Flow: vehicle calls normal phases, use case calls in bkg capture heat/image etc, processed by AI/algo -> wildfire detected -> use case calls take priority, send coordinate and image to ground station
